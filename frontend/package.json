{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p $(grep '^PORT=' ../.env | cut -d '=' -f 2 || echo 3000)",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "next-intl": "^4.9.1"
  }
}
